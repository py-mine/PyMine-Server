from src.logic.command import command as command

# Events:
#   command: @command(name='name', node='plugin_name.cmds.cmd_name')
#   packets:
#       packet logic: @packet_logic(id=0x00)
#       after packet logic: @after_packet_logic(id=0x00)
